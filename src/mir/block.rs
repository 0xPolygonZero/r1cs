use gadget_builder::GadgetBuilder;
use linear_combination::LinearCombination;

/// Returns the new Merkle root of the record commitment tree.
fn mir_block(builder: &mut GadgetBuilder, old_root: LinearCombination) -> LinearCombination {
    unimplemented!("TODO")
}